{% macro quickLink(title, image, link) %}
    <li class="media mb-5">
        {% if link %}
            <a class="d-block" href="{{ link }}" target ="_blank" title = "{{ title }}">
                <img src="{{ image }}" class="mr-3 side-link" alt="{{ title }}"></a>
        {%  else %}
            <img id="noonan_tree1" src="{{ image }}" class="mr-3 side-link" title = "View image"
                 onclick = "viewImage(this.src)">
        {% endif %}
        <div class="media-body">{{ title }}</div>
    </li>
{% endmacro %}