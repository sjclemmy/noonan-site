{% from "right_hand_menu/quickLink.njk" import quickLink %}

         <div class="list-group list-group-flush">
		 <h6 class="text-center" style="margin:0;padding:0;background-color:beige; font-size: 1.2em;text-decoration:none;">
		 QuickLinks</h6>
        {% for item in quickLinks %}
            {{ quickLink(item.title, item.image, item.link) }}
        {% endfor %}
		</div>
    
